// length property
let string = "some number";
console.log(string.length);
// prompt for string
let max = 145;
let textUsed = prompt("Give me some text");
alert(
  `That is ${textUsed.length} , you have ${145 - textUsed.length} remaining`
);
